# dependencies
node_modules
.pnp
.pnp.js

# build
dist
dist-ssr
*.local

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# env
# 비밀이 든 것만 막고, 예시와 개발용 기본값은 공유한다
# (VITE_ 값은 어차피 번들에 박히므로 비밀을 두면 안 된다)
.env
.env.local
.env.production
!.env.development
!.env.*.example

# large assets — npm run fetch-data 로 언제든 다시 만든다 (합계 약 25MB)
public/geo/*.geojson
public/geo/*.json
public/geo/regions/
public/textures/*.jpg
public/textures/*.png
