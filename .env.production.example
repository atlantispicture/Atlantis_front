# 배포용 예시 — 실제 값으로 채워 .env.production 으로 복사한다.
# (.env.production 은 커밋하지 않는다 — .gitignore 확인)
#
# 주의: Vite 는 VITE_ 로 시작하는 값만 번들에 넣는다. 그리고 이 값은
# 클라이언트 코드에 그대로 박히므로 비밀은 절대 두지 않는다.

VITE_API_BASE=https://api.example.com
